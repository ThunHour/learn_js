const createPerson=(name,age,gender) => ({name,age,gender});
console.log(createPerson("seakkihour",19,'M'))
//so when we do { name,age,gender} is mean those name,age,gender is the key so the order of parameter that you pass is the values